name: Docker Image CI for GHCR
on:
  push
jobs:
 build_and_publish:
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v2
     - name: Build and push the image
       run: |
        docker login --username nosheensharif --password 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 ghcr.io
        docker build . --tag ghcr.io/nosheensharif/hello-world-ghcr:latest
        docker push ghcr.io/nosheensharif/hello-world-ghcr:latest


